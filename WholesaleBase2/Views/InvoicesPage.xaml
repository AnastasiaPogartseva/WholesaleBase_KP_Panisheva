<Page x:Class="WholesaleBase2.Views.InvoicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WholesaleBase2.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     Title="InvoicesPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Накладные" FontSize="28" Foreground="Black" 
                   FontWeight="Light" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="➕ Создать накладную" Name="Add" Click="Add_Click"  Width="150" Height="38" Margin="0,0,10,0"/>
            <Button Content="👁️ Просмотреть" Name="View" Click="View_Click"  Width="120" Height="38" Margin="0,0,10,0"/>
            <Button Content="✏️ Изменить" Name="Edit" Click="Edit_Click"  Width="120" Height="38" Margin="0,0,10,0"/>
            <Button Content="🗑️ Удалить" Name="Delete" Click="Delete_Click"  Width="120" Height="38" Margin="0,0,10,0"/>
            <Button Content="🔄 Обновить" Name="Refresh" Click="Refresh_Click"  Width="120" Height="38"/>
        </StackPanel>

        <DataGrid x:Name="dgInvoices" Grid.Row="2" AutoGenerateColumns="False" 
                  SelectionMode="Single" IsReadOnly="True" MouseDoubleClick="dgInvoices_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding InvoiceNumber}" Width="120"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding InvoiceDate, StringFormat={}{0:dd.MM.yyyy}}" Width="120"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding CustomerName}" Width="200"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat={}{0:N2} ₽}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>