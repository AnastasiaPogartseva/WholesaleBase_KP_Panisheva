<Window x:Class="WholesaleBase2.Views.InvoiceViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WholesaleBase2.Views"
        mc:Ignorable="d"
        Title="Просмотр накладной" 
        Width="600" Height="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="10" Background="White" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Просмотр накладной" 
                       FontSize="20" FontWeight="SemiBold" 
                       Foreground="#5E4B3C" Margin="0,0,0,20"/>

            <!-- Информация о накладной -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер:" FontWeight="Bold" Margin="0,0,10,5"/>
                <TextBlock x:Name="txtNumber" Grid.Row="0" Grid.Column="1" Margin="0,0,20,5"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Дата:" FontWeight="Bold" Margin="0,0,10,5"/>
                <TextBlock x:Name="txtDate" Grid.Row="0" Grid.Column="3" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Клиент:" FontWeight="Bold" Margin="0,5,10,0"/>
                <TextBlock x:Name="txtCustomer" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,5,0,0"/>
            </Grid>

            <!-- Таблица товаров -->
            <DataGrid x:Name="dgItems" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Товар" Binding="{Binding ProductName}" Width="300"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:N2} ₽}" Width="100"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Total, StringFormat={}{0:N2} ₽}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Итого -->
            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" Text="Итого:" FontSize="16" FontWeight="Bold" 
                           VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock x:Name="txtTotal" Grid.Column="1" Text="" FontSize="16" 
                           FontWeight="Bold" VerticalAlignment="Center"/>
            </Grid>

            <!-- Кнопка закрытия -->
            <Button Grid.Row="3" Content="Закрыть" Name="Close" Click="Close_Click" 
                    Width="100" Height="35" HorizontalAlignment="Right" Margin="0,50,0,0"/>
        </Grid>
    </Border>
</Window>